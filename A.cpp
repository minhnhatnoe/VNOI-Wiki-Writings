#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

signed main(){
    cin.tie(0)->sync_with_stdio(0);
    for (int i=-1; i>=-100; i--){
        cout << i << " " << (i >> 1) << "\n";
    }
}